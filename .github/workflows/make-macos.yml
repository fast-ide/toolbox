name: ci_makefile
on:
  push:
    branches:
      - master
jobs:
  build:
    name: build
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@master
      - name: build
        run: |
          make install
